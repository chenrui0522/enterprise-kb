## 1. 转换报告与缓存基础

- [x] 1.1 `document_versions` 增加转换报告字段并迁移，验证 `uv run alembic upgrade head` 成功
- [x] 1.2 流水线记录转换器/版本/分诊依据/降级/页数/表格数/图片数/耗时，验证文档状态可查询到报告
- [x] 1.3 实现解析缓存（文件哈希 + 转换器 + 版本 + 参数）与命中复用，验证重复入库不重复解析

## 2. 分诊与路由

- [x] 2.1 实现轻量分诊：按页判断文字层、图片与表格存在性，验证分诊结果可复现
- [x] 2.2 按格式构建候选转换器链并支持回退，验证首选失败时自动切换
- [x] 2.3 逐页 OCR 回退：无文字层的页面单独走 OCR，验证混合扫描件不丢页

## 3. Docling 独立服务

- [x] 3.1 `docker-compose.yml` 新增 docling 服务与模型缓存卷、健康检查，验证服务可启动
- [x] 3.2 实现 Docling HTTP 客户端与结构化结果映射，验证返回内容进入统一结构
- [x] 3.3 服务不可用或失败时自动跳过该候选，验证不影响转换完成

## 4. XLSX 语义化

- [x] 4.1 按 sheet 分节并推断表头，验证多 sheet 输出章节与字段名
- [x] 4.2 逐行生成语义文本并保留表格元数据，验证不重复入索引
- [x] 4.3 合并单元格填充、公式取值与超长表分组，验证边界用例

## 5. 文档与验证

- [x] 5.1 README 增加转换分诊、Docling 部署与转换报告说明，验证命令与实际一致
- [x] 5.2 各格式回归用例（文本 PDF/扫描件/含表 PDF/DOCX/XLSX）验证转换均有输出
- [x] 5.3 基线评测：解析耗时、结构正确性与检索召回对比，验证引入 Docling 的收益可量化
- [x] 5.4 运行 `uv run pytest -q`，验证全部通过