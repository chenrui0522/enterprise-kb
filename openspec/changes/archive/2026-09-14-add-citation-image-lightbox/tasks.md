## 1. 缩略图接口

- [x] 1.1 图片接口支持 `w` 参数并按需生成缩略图，缓存到本地目录，验证命中缓存
- [x] 1.2 宽度白名单与上限约束，验证超范围请求被归一到允许档位
- [x] 1.3 不可缩放格式回退原图，验证响应仍可用
- [x] 1.4 缩略图接口测试，验证尺寸与 content-type 正确

## 2. 前端浏览

- [x] 2.1 引用区缩略图改用缩略图地址并懒加载，验证首屏不加载原图
- [x] 2.2 新增站内 lightbox（放大、上一张/下一张、Esc、点击遮罩关闭），验证交互可用
- [x] 2.3 lightbox 展示图片说明并保留原图入口，验证文案与链接正确
- [x] 2.4 图片加载失败时隐藏缩略图并保留文字引用，验证降级行为

## 3. 验证

- [x] 3.1 前端 `npm run build` 通过
- [x] 3.2 全量测试 `uv run pytest -q` 通过