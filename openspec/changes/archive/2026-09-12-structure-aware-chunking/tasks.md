## 1. 文档结构抽取层

- [x] 1.1 新增文档结构与块模型（块类型、层级、页码、行号、表格行列），并为元数据字段编写单元测试，验证 `uv run pytest tests -k structure -q` 通过
- [x] 1.2 改造 Word 转换器：识别中英文标题样式、按文档顺序输出表格、固定合并单元格展开规则，验证新增转换器测试覆盖标题层级与表格顺序
- [x] 1.3 改造 PDF 转换器：基于字体信息推断标题层级、表格按页内位置插入正文顺序且不与正文重复，验证含正文与表格的 PDF 测试通过
- [x] 1.4 Markdown/Office 转换器补充列表与表格块类型标注，结构抽取失败时降级为段落级并记录降级标记，验证降级路径测试通过

## 2. 类型感知切片器

- [x] 2.1 建立切片策略路由（doc_type → chunker）：faq/policy/sop/table 各自实现，未识别类型回退现有 `chunk_markdown`，验证路由与回退测试通过
- [x] 2.2 实现 FAQ 切片器：问答对成块、不跨对合并、超长答案按 `faq_id` 拆子块，验证问答对边界测试通过
- [x] 2.3 实现制度条款切片器：按条/款成块、记录 `clause_no` 与完整 `heading_path`、超长条款按句界拆子块，验证条款归属与章节路径测试通过
- [x] 2.4 实现 SOP 步骤切片器：按步骤成块、记录 `step_no`、前置条件与步骤同块且步骤内部不拆分，验证步骤完整性测试通过
- [x] 2.5 实现表格切片器：超容量按行组拆分并重复表头、记录 `table_id` 与行号范围，验证表格切片均含表头与行号范围
- [x] 2.6 扩展切片元数据模型（`chunk_type`/`heading_path`/`clause_no`/`step_no`/`faq_id`/`table_id`/`parent_id`/`chunker_version`），验证元数据序列化测试通过

## 3. 存储与元数据

- [x] 3.1 扩展 `ChunkRecord` 与 Milvus schema：新增元数据字段并保留 dense + BM25(text)，验证新集合插入与混合检索测试通过
- [x] 3.2 新增数据库迁移：`document_versions` 增加 `doc_type` 与 `chunker_version`，验证 `uv run alembic upgrade head` 成功
- [x] 3.3 改造入库流水线：携带 `doc_type` 与 `chunker_version`、写入新元数据字段，验证 pipeline 测试覆盖新字段
- [x] 3.4 上传接口新增可选 `doc_type` 参数与非法值校验，并持久化到文档版本，验证接口测试覆盖合法/非法取值

## 4. 全量重导能力

- [x] 4.1 新增重导入口（扫描文档版本与原文存储、校验原文存在性、支持 `--dry-run` 与入队），验证 dry-run 能列出当前 1 份文档且缺失原文被标记为跳过
- [x] 4.2 重导幂等处理：写入前按 `version_id` 清理当前集合旧切片再插入，验证同一文档重导两次切片数量不翻倍
- [x] 4.3 输出重导摘要（成功/失败/跳过、切片数量、切片器版本），验证摘要内容可追溯到文档版本

## 5. 集合切换与重建验证

- [x] 5.1 `.env` 与 `.env.example` 增加新集合名配置并切换为 `kb_chunks_v2`（旧集合保留），验证配置加载后目标集合名正确
- [x] 5.2 启动 Docker Desktop 与 compose 服务，确认 bge-m3 与 bge-reranker-v2-m3 以 sentence-transformers 引擎在线，验证 `/v1/models` 同时包含两个模型
- [x] 5.3 对现有 PDF 执行重导（先 dry-run 后全量），验证新集合切片数量、抽样检索与引用定位正确
- [x] 5.4 运行完整测试套件 `uv run pytest -q`，验证全部通过
- [x] 5.5 用 20-50 条真实制度/SOP/FAQ 问题对比改造前后的召回命中，验证关键问题命中率不下降（按用户决定仅测新策略：50 题 gold set，recall@3=0.979、MRR=0.948；唯一未命中为题目标注歧义，已限定为企业简介口径并单题复测命中）
- [x] 5.6 重导验证通过后，就"是否删除旧集合 `kb_chunks`"单独取得确认，验证删除前已完成新集合回滚演练（已先备份 45 块到 data/backups/kb_chunks-20260912-155109.json，再经用户确认删除）
