## 1. 数据模型与迁移

- [ ] 1.1 新增 `chunk_parents` 与 `document_tables` 表及迁移，验证 `uv run alembic upgrade head` 成功
- [ ] 1.2 `document_versions` 记录切片器版本与父子/表格统计，验证文档状态可查询

## 2. 父子分块器

- [ ] 2.1 按标题层级构建父块并记录章节路径与页码，验证多级标题用例
- [ ] 2.2 父块内递归生成子块并写入 `parent_id`，验证子块不跨父块
- [ ] 2.3 超长子树切二级父块，验证章节路径保持一致
- [ ] 2.4 表格与图片作为原子块，验证不被跨块拆分
- [ ] 2.5 切片器版本升级为 `structure-v2`，验证切片元数据包含版本与父块标识

## 3. 存储与入库

- [ ] 3.1 父块写入 Postgres、子块写入 Milvus，验证父子数量一致且可相互追溯
- [ ] 3.2 表格资产落库并与子块 `table_id` 关联，验证可按标识取回整表
- [ ] 3.3 重复入库与重导前清理旧父块/表格资产，验证不产生孤儿数据

## 4. 检索与生成

- [ ] 4.1 检索返回子块并携带 `parent_id`，验证父块标识可解析
- [ ] 4.2 生成端父块展开与去重，验证同一父块只用一次
- [ ] 4.3 父块 token 预算与截断规则，验证超长父块不溢出
- [ ] 4.4 引用保持子块精度并可携带图片/表格资产，验证引用定位不变粗

## 5. 重导、评测与文档

- [ ] 5.1 全量重导现有语料并核对父子与表格数量，验证无失败文档
- [ ] 5.2 评测：子块召回与答案质量对比改造前后，验证关键问题不下降
- [ ] 5.3 README 说明父子分块与表格资产，验证与实际行为一致
- [ ] 5.4 运行 `uv run pytest -q`，验证全部通过
