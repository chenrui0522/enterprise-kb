## 1. 数据模型与迁移

- [x] 1.1 新增 `chunk_parents` 与 `document_tables` 表及迁移，验证 `uv run alembic upgrade head` 成功
- [ ] 1.2 统一 chunk schema 增加 `chunk_kind`/`row_index` 等字段，验证新集合可写入与检索
- [x] 1.3 `document_versions` 记录切片器版本与父子/表格统计，验证文档状态可查询

## 2. 父子分块器

- [x] 2.1 按标题层级构建父块并记录章节路径与页码，验证多级标题用例
- [x] 2.2 父块内递归生成子块并写入 `parent_id`，验证子块不跨父块
- [x] 2.3 超长子树切二级父块，验证章节路径保持一致
- [x] 2.4 表格与图片作为原子块不跨块拆分，验证边界用例
- [x] 2.5 切片器版本升级为 `structure-v2`，验证元数据包含版本与父块标识

## 3. 表格行级与摘要

- [x] 3.1 表格行级语义化 chunk（字段=值、表头字段名、行号），验证检索可命中单行
- [x] 3.2 表级摘要 chunk（表名/列/行数/单位），验证可检索到整表概要
- [x] 3.3 整表 JSON 落对象存储并可按 `table_id` 取回，验证取回内容与源表一致
- [x] 3.4 表格不参与父子分块，验证表格文档不产生 parent 展开

## 4. 存储与入库

- [x] 4.1 父块写入 Postgres、child/表格/图片 chunk 写入 Milvus，验证数量与关联一致
- [x] 4.2 原图与整表 JSON 落 `images/`、`tables/`，验证路径与元数据对应
- [x] 4.3 重导前清理旧父块与表格资产，验证无孤儿数据

## 5. 检索与生成

- [x] 5.1 检索保持两路 RRF 并支持元数据过滤条件，验证过滤生效且不改变融合逻辑
- [x] 5.2 生成端父块展开与去重，验证同一父块只用一次
- [x] 5.3 父块 token 预算与截断规则，验证超长父块不溢出
- [x] 5.4 表格/图片 chunk 不触发父块展开，验证使用自身文本
- [x] 5.5 引用保持子块精度并可携带图片/表格资产，验证定位不变粗

## 6. 配置、重导与评测

- [x] 6.1 按 `doc_type` 的切块参数配置（含默认值），验证覆盖生效
- [ ] 6.2 全量重导并核对父块/子块/表格/图片数量，验证无失败文档
- [ ] 6.3 评测：子块召回与答案质量对比，验证关键问题不下降
- [x] 6.4 README 说明父子分块、表格两级处理与存储职责，验证与实际一致
- [x] 6.5 运行 `uv run pytest -q`，验证全部通过
