## 1. 数据模型与存储

- [x] 1.1 新增 `document_images` 模型与 Alembic 迁移（含租户、版本、页码、bbox、哈希、尺寸、说明字段），验证 `uv run alembic upgrade head` 成功
- [x] 1.2 实现内容寻址图片存储与 sha256 去重，验证重复图片只落一份
- [x] 1.3 实现装饰性图片过滤（最小边长/字节阈值），验证小图标被跳过
- [x] 1.4 版本替换或重导时清理旧版本图片，验证旧文件被移除

## 2. 图片抽取

- [x] 2.1 MinerU 客户端支持 ZIP 响应，解析 `content_list.json` 的图片条目（img_path/page/bbox/caption），验证含图 PDF 能拿回图片清单
- [x] 2.2 实现本地兜底抽取（PyMuPDF / python-docx / python-pptx），验证 MinerU 不可用时仍有图片产出
- [x] 2.3 入库流水线接入图片抽取与落盘登记，验证含图文档状态为 ready 且图片表有记录
- [x] 2.4 抽取失败或未启用时写入告警与文档状态，验证不再静默丢图

## 3. 结构、切片与索引

- [x] 3.1 结构模型新增 image 块类型，验证图片块可被解析与序列化
- [x] 3.2 切片器生成 `chunk_type=image` 的切片并携带 `image_id`，无文本时使用占位文本，验证不产生零文本切片
- [x] 3.3 `ChunkRecord`/`SearchHit` 增加 `image_id` 字段，验证序列化与检索返回
- [x] 3.4 Milvus schema 增加 `image_id` 并切换到新集合版本，验证新集合可写入与检索
- [x] 3.5 全量重导适配新集合与图片清理，验证重导后图片与切片一致

## 4. 接口与引用

- [x] 4.1 新增图片读取接口（租户与文档归属校验、Content-Type、缓存头），验证非授权租户被拒绝
- [x] 4.2 引用结构携带 `images` 列表，验证命中图片证据时引用包含图片标识
- [x] 4.3 SSE `done` 事件与消息持久化包含图片引用，验证前端能拿到图片信息
- [x] 4.4 历史消息缺少图片字段时按空处理，验证旧数据仍可正常展示

## 5. 前端展示

- [ ] 5.1 答案引用区展示图片缩略图并支持点击放大，验证加载失败时降级为文字引用
- [ ] 5.2 文档详情页展示该文档的图片列表，验证可浏览与放大

## 6. 测试与文档

- [x] 6.1 新增抽取、去重、过滤、版本清理的单元测试，验证全部通过
- [x] 6.2 新增图片接口与引用组装的集成测试，验证权限与返回结构
- [x] 6.3 回归：不含图片的文档不受影响，验证既有测试全部通过
- [ ] 6.4 README 补充图片抽取与展示说明，验证命令与配置一致

## 7. 验证

- [ ] 7.1 端到端验证：上传含图文档 → ready → 提问命中图片 → 引用中展示图片
- [x] 7.2 运行 `uv run pytest -q`，验证全部通过