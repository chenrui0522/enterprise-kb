## 1. 数据模型与迁移

- [ ] 1.1 `messages` 增加 `compressed`/`token_count`/`summary_id`，验证迁移后历史数据默认未压缩
- [ ] 1.2 新增 `conversation_summaries` 表（覆盖范围、内容、token、版本），验证可保存多版本摘要
- [ ] 1.3 会话历史查询同时返回摘要与未压缩消息，验证接口可用

## 2. 记忆压缩服务

- [ ] 2.1 token 估算与预算配置，验证超预算判定可复现
- [ ] 2.2 滚动摘要生成（旧摘要 + 新滑出消息），验证摘要覆盖范围正确
- [ ] 2.3 压缩只标记不删除并移出窗口，验证原文仍可查询
- [ ] 2.4 同会话压缩加锁与事务，验证并发不重复压缩

## 3. 上下文加载

- [ ] 3.1 三层拼接加载（摘要 + 未压缩最近消息 + 当前问题），验证顺序与内容正确
- [ ] 3.2 查询改写使用拼接上下文，验证追问能延续此前话题
- [ ] 3.3 无摘要/无历史时退化路径，验证不报错

## 4. 会话标识与会话状态

- [ ] 4.1 服务端校验会话标识归属并对无效标识返回明确错误，验证不静默新建
- [ ] 4.2 前端会话标识持久化（URL + localStorage）并每次请求携带，验证刷新页面后仍为同一会话
- [ ] 4.3 明确消息内容以数据库为准、checkpointer 只存运行态，验证恢复不依赖缓存

## 5. 验证与文档

- [ ] 5.1 跨天恢复自动化验收：清空 Redis → 重启服务 → 同 session_id 追问，验证上下文延续
- [ ] 5.2 摘要质量抽样检查（关键实体/编号保留），验证摘要可支撑追问
- [ ] 5.3 README 说明记忆分层、压缩与恢复行为，验证与实际一致
- [ ] 5.4 运行 `uv run pytest -q`，验证全部通过
