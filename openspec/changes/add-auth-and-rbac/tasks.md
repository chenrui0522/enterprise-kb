## 1. 数据模型与迁移

- [ ] 1.1 新增 users / roles / permissions / user_roles / sessions 数据模型，验证 `uv run pytest -q` 中模型相关测试通过
- [ ] 1.2 新增 Alembic 迁移（建表 + 种子四种角色与权限映射），验证 `uv run alembic upgrade head` 成功
- [ ] 1.3 编写历史数据迁移策略：历史会话与审计标记 legacy 并归属初始管理员，验证迁移后无 owner 为空的数据

## 2. 认证核心

- [ ] 2.1 实现 argon2id 密码哈希与校验模块，验证哈希不可逆且校验用例通过
- [ ] 2.2 实现服务端会话的签发、校验、续期与吊销，验证会话过期与登出后失效用例通过
- [ ] 2.3 新增登录 / 登出 / 当前用户接口，验证登录成功返回用户与权限集合
- [ ] 2.4 登录限流与连续失败锁定，验证达到阈值后登录被拒绝
- [ ] 2.5 登录成功、失败、登出写入审计且记录真实用户标识，验证审计条目内容正确

## 3. 授权与租户

- [ ] 3.1 实现 `require_permission` 授权依赖与权限映射，验证示例接口的 401/403 行为
- [ ] 3.2 将文档、问答、审计、系统路由接入授权依赖，验证未授权访问被拒绝
- [ ] 3.3 租户改为从登录用户解析并移除对 `X-Tenant-ID` 的信任，验证伪造请求头不影响数据范围
- [ ] 3.4 会话与审计归属真实用户，验证用户只能看到自己的会话且审计 actor 正确

## 4. 管理员引导与运维

- [ ] 4.1 提供首个管理员引导命令（或首次启动环境变量），验证空库可创建管理员且无需默认口令
- [ ] 4.2 `.env.example` 与 `.env` 增加认证相关配置（会话有效期、Cookie 安全开关、引导字段占位），验证配置加载

## 5. 前端登录与权限

- [ ] 5.1 新增登录页与鉴权上下文，验证未登录访问受保护路由会跳转登录
- [ ] 5.2 路由守卫与权限驱动 UI（菜单/按钮按权限显示），验证 viewer 看不到上传入口
- [ ] 5.3 前端请求统一携带 Cookie 并处理 401/403，验证会话过期时自动回到登录页

## 6. 测试与文档

- [ ] 6.1 既有接口测试改为注入已认证测试用户，验证全量测试通过
- [ ] 6.2 新增认证/授权/登录保护/会话归属测试，验证 401、403、锁定、归属用例覆盖
- [ ] 6.3 README 增加登录、角色权限与首个管理员创建说明，验证说明与实际命令一致

## 7. 验证

- [ ] 7.1 端到端手工验证：创建管理员 → 创建 editor 与 viewer → editor 可上传、viewer 上传被拒 → 提问与引用正常
- [ ] 7.2 运行 `uv run pytest -q`，验证全部通过